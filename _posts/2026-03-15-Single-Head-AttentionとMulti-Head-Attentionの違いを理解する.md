---
layout: article
title: Single Head AttentionとMulti Head Attentionの違いを理解する
author: K. Kotaro
tags: []
date: '2026-03-20'
published: false
---
# Attentionメカニズムの仕組みと、Multi-Headでよくある「勘違い」

自然言語処理のモデルで必ず登場する「Attention」。
数式がたくさん出てきて難しく感じますが、その目的はシンプルで、**(単語1)と(単語2)の関連性を知りたい**というものです。

この記事では、基本となる「Single Head Attention」から「Multi Head Attention」への発展、そして多くの人が陥りがちな**決定的な勘違い**について、数式を交えながら分かりやすく解説します！

---


まず、単語はコンピュータで解析できるように、多次元のベクトルに変換されます。

非常に大雑把に言えば、鯨は「哺乳類」かつ「水のなかにいる」ので、(1, 1)とし、ヒトは「哺乳類」ですが「基本陸上にいる」ので(1, 0)とする感じです。このとき、例えば(0, 1)を考えると、これは「哺乳類ではない」かつ「水の中にいる」ので、魚などが該当します。

これは非常に大雑把な説明のため、2次元で0と1しか用いていませんが、実際は数百〜数千次元のベクトルに変換されます。例えばGPT-3では、12,288次元のベクトルに変換されているようです。

単語の各成分ごとに、なんらかの要素をどれくらい含むか、が入っています。似た単語はベクトルが近い位置に、全く異なる単語は遠い位置におかれます。

この部分についてはこれ以上深く解説しません。単語をベクトルに変換した後の、以下の$n$次元の2つのベクトルについて考えていきます。この2つの単語の関連性を求めるのが目標です。

(単語1) $=(t_{11},t_{12},t_{13},...,t_{1n})$   
(単語2) $=(t_{21},t_{22},t_{23},...,t_{2n})$ 

### 前提条件と次元数
* 単語数 $2$(後の説明では、これを$ N_{words}$と表記します)
* 単語ベクトルの次元 $d_{model}=n$  
* キー、バリューベクトルの次元数 $d_{k}=d_{v}$ とする  
$(d_{k}=d_{v}=n$ とすると理解しやすいので、読むときは$n$ として読むと良い)    
(一般的には $d_{model}$ 未満に設定することが多い)  
---

## 1. Single Head Attentionの基礎

まずは基本となる Single Head Attention から見ていきましょう。

Attentionの計算には、「クエリ (Q)」「キー (K)」「バリュー (V)」という3つのベクトルを使います。

### Q, K, Vの作り方
単語ベクトルに対して、**学習可能な**重み行列である $W_Q, W_K, W_V$ を掛け合わせることで、Q, K, V の行列を作ります。これらの重みを学習させる、させておくことで、モデルの性能が向上します。


$Q = \begin{bmatrix} (単語1)  \\ (単語2)  \end{bmatrix}$  




### Attentionの計算式
Q, K, V が準備できたら、以下の有名な式でAttentionを計算します。

$$Attention(Q,K,V)=softmax(\frac{QK^{T}}{\sqrt{d_{k}}})V=h$$

この計算結果によって、単語間の関係性が抽出されます。

---

## 2. Multi Head Attentionへの進化

次に、より表現力の高い Multi Head Attention についてです。

ここでは、次元をヘッド数 $h$ で分割し、1つのヘッドあたりの次元数を $d_{k}=d_{v}=\frac{d_{model}}{h}=d_{head}$ として計算します。

### なぜヘッドを分けるのか？
ヘッドごとに異なる重み（例えば $W_{v}$ など）を持たせることで、捉える特徴量もそれぞれ違うものになるからです。

各ヘッド $i$ において、先ほどと同じようにAttentionを計算します。

$$Attention(Q_{i},K_{i},V_{i})=softmax(\frac{Q_{i}K_{i}^{T}}{\sqrt{d_{head}}})V_{i}=h_{i}$$

### 結果の結合（Concat）と最終出力
すべてのヘッドで計算した結果 $h_1, h_2, ..., h_h$ は、最後に結合（Concat）されます。

$$h=Concat(h_{1},h_{2},...,h_{h})$$

そして最後に、学習可能な行列 $W_O$ を掛け合わせることで、最終出力が得られます。

---

## 3. 【重要】Multi Head Attentionの「よくある勘違い」

ここからがこの記事で一番伝えたいポイントです。
Multi Head Attentionの実装や概念を理解する際、実は**2つの正しい考え方**と、**1つのよくある勘違い**が存在します。

### 2つの「正しい」考え方
以下の2つは、アプローチが違うだけでどちらも同じことを行っています。

1. **小さい重みを用意する考え方（論文の手法）:**
   小さい $W_Q, W_K, W_V$ を $h$ 個ずつ用意して、それをそれぞれ単語のベクトルにかけて、異なる特徴量に注目しながら分析しているという考え方 [cite: 92, 93][cite_start]。「Attention is All You Need」論文ではこの考え方が用いられています。
2. **大きな行列を分割する考え方:**
   単一の大きな重みからできたQ, K, Vを、それぞれ縦に $h$ 等分して、それぞれで計算しているという考え方 。

### ⚠️よくある間違い⚠️
上記の2つの考え方に混乱し、以下のように間違えてしまう人が非常に多いです。

* ❌ **間違い:** 「単語のベクトルを縦に $h$ 等分して重さをかけている」。

 

* ⭕ **正解:** 「単語のベクトルに $h$ 等分した重さをかけている」または「重さをかけたあとに縦に $h$ 等分している」。

重さをかけた後、行列の各成分には、**単語のすべての成分情報**がしっかり入っています（これは内積の定義から明らかです）。単語ベクトルそのものを最初にぶつ切りにしてしまうわけではない点に注意しましょう！
